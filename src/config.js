// Configuration for LiveKit components
window.process = {
  env: {
    NODE_ENV: 'development',
    REACT_APP_DISABLE_WARNINGS: 'true'
  }
};

export const config = {
  isDevelopment: true,
  disableWarnings: true
}; 